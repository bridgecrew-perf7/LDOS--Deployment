---
 - hosts: "{{ groups['installer'][0] }}"
   gather_facts: true
   tasks:
   - name: Clone a github repository
     git:
       repo: https://github.com/jporeilly/LDOS--Installation.git
       dest: /installer/Workshop-LDOS
       clone: yes
       update: yes